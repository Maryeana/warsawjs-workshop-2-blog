(function (root) {
    'use strict';

    class PostList {
        constructor() {
            this.posts = [];
        }

        addPostModel(postModel) {
        }

        getPostModelById(id) {
        }
    }

    root.blog.models.PostList = PostList;
}(window));
